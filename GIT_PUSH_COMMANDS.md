# Git Push Quick Commands

Use this whenever you make new changes.

```bash
cd "/Users/suryaadhikari/Documents/capstone"
git status
git add .
git commit -m "your commit message"
git push origin main
```

If your branch is behind remote:

```bash
git pull --rebase origin main
git push origin main
```
